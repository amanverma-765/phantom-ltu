<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Cache directory for temporary APK files during installation -->
    <cache-path name="install_cache" path="." />
    <!-- Files directory for patched APKs -->
    <files-path name="patched" path="patched/" />
    <!-- External cache for any overflow -->
    <external-cache-path name="external_cache" path="." />
</paths>
